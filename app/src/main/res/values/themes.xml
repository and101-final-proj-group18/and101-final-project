<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.And101finalproject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.And101finalproject" parent="Base.Theme.And101finalproject" >
        <item name="colorPrimary">@color/primary_500</item>
        <item name="colorPrimaryVariant">@color/primary_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="android:windowBackground">@color/white</item>
        <item name="android:colorBackground">@color/white</item>

        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.splashScreen" parent="Base.Theme.And101finalproject" >
        <item name="android:windowBackground">@color/primary_500</item>
        <item name="android:statusBarColor">@color/primary_500</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="display" >
        <item name="android:textSize">45sp</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="title" >
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="body" >
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="search_bar" >
        <item name="android:ems">14</item>
        <item name="android:textStyle">normal</item>
        <item name="android:hint">@string/search_prompt</item>
        <item name="android:textColorHint">@color/greyscale</item>
        <item name="android:inputType">textVisiblePassword</item>
        <item name="android:drawableStart">@drawable/icon_start</item>
        <item name="android:drawableEnd">@drawable/icon_end</item>
    </style>
</resources>